{% extends 'admin/base.twig' %}

{% block body %}
    <table id="table_id">
        <thead>
        <tr>
            <th>Название</th>
            <th>Тип весов</th>
            <th>Количество датчиков</th>
            <th></th>
            <th></th>
        </tr>
        </thead>

        <tbody>
        {% if tkpList %}
            {% for item in tkpList %}
                <tr>
                    <td>{{ item.name }}</td>
                    <td>{{ item.model }}</td>
                    <td>{{ item.count_sensor }}</td>
                    <td>
                        <a href="/admin/tkp-editor/update/{{ item.id }}">
                            <button class="tkp-btn update-tkp" data-item="{{ item.id }}" data-type="update"><img src="/assets/img/icons/admin-icons/edit.svg" alt=""></button>
                        </a>
                    </td>
                    <td>
                        <button class="tkp-btn delete-tkp" data-item="{{ item.id }}" data-type="update"><img src="/assets/img/icons/admin-icons/delete.svg" alt=""></button>
                    </td>
                </tr>
            {% endfor %}
        {% endif %}
        </tbody>
    </table>
{% endblock %}